@model EventDetailViewModel
<div class="card shadow-sm mb-4">
    <div class="card-header fw-semibold bg-light">Waveform Viewer</div>
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <div><strong>Computed Δt:</strong> @Model.DeltaTMilliseconds.ToString("0.0") ms</div>
            <div class="small text-muted">Seismic onset: @Model.SeismicOnsetIndex | Acoustic onset: @Model.AcousticOnsetIndex</div>
        </div>
        <canvas id="waveformChart" height="140"></canvas>
    </div>
</div>
